services:
  - type: web
    name: mijn-app
    env: python
    pythonVersion: 3.11
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python dmcapp.py
